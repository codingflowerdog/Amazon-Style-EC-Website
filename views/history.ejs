<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <title>HappyMall</title>
    <%include common.ejs%>
    <link rel="stylesheet" href="/css/history.css">
</head>
<body>
    <%include header.ejs%>
    <div class="container">
        <div class="card bg-light history">
            <article class="card-body mx-auto" style="width:100%;">
                <h4 class="card-title mt-3 text-center">주문이력</h4>
                <%  if(productInfo.orderHistory && session.accountEmail){
                        for(var i=0; i< productInfo.orderHistory.length;i++){%>
                    <div class="media border p-2">
                        <img src="/uploads/<%= productInfo.orderHistory[i]._doc.filename%>" class="align-self-start mr-3" style="width:120px">
                        <div class="media-body">
                            <h4><%= productInfo.orderHistory[i]._doc.title%></h4>
                            <p><%= productInfo.orderHistory[i]._doc.price%></p>
                        </div>
                        <div class="d-flex flex-column">
                            <div class="p-1">
                                <a href="/procOrder?id=<%= productInfo.orderHistory[i]._doc._id %>">
                                    <button type="button" class="btn order">상품 재구매하기</button>
                                </a>
                            </div>
                            <div class="p-1">
                                <a href="/procDeleteHistory?id=<%= productInfo.orderHistory[i]._doc._id %>">
                                    <button type="button" class="btn order">구매이력 삭제</button>
                                </a>
                            </div>
                        </div>
                    </div>
                <%      }
                    }%>
            </article>
        </div>
    </div>
</body>
</html>